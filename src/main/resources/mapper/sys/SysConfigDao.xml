<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">    
<mapper namespace="io.sbed.modules.sys.dao.SysConfigDao">
	<select id="queryObject" resultType="io.sbed.modules.sys.entity.SysConfig">
		select * from sys_config where id = #{id}
	</select>
	
	<select id="queryList" parameterType="map" resultType="io.sbed.modules.sys.entity.SysConfig">
		select c.* from sys_config c
		<where>
			<if test="key != null and key != ''">
				and c.`key` like concat('%',#{key},'%')
			</if>
		</where>
		<choose>
			<when test="sidx != null and sidx != ''">
				order by c.${sidx} ${order}
			</when>
			<otherwise>
				order by c.id desc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
	<select id="queryTotal" parameterType="map" resultType="int">
		select count(*) from sys_config c
		<where>
			<if test="key != null and key != ''">
				and c.`key` like concat('%',#{key},'%')
			</if>
		</where>
	</select>
	
	<insert id="save" parameterType="io.sbed.modules.sys.entity.SysConfig">
		insert into sys_config(`key`, `value`, remark)
			values(#{key},#{value},#{remark})
	</insert>
	
	<update id="update" parameterType="io.sbed.modules.sys.entity.SysConfig">
		update sys_config set `key` = #{key}, `value` = #{value}, remark = #{remark} 
			 where id = #{id}
	</update>
	
	<!-- 根据key，查询 -->
	<select id="queryObjectByKey" parameterType="string" resultType="io.sbed.modules.sys.entity.SysConfig">
		select * from sys_config where `key` = #{key}
	</select>
	
	<delete id="deleteBatch" parameterType="int">
		delete from sys_config where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>
	
</mapper>